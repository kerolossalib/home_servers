services:
  homeassistant:
    container_name: homeassistant
    image: homeassistant/home-assistant:stable
    restart: always
    ports:
      - "8123:8123"
    privileged: true
    volumes:
      - ./config:/config
